version: 0.2

phases:
  install:
    commands:
      - echo "Installing dependencies"
      - npm install
  build:
    commands:
      - echo "Running tests inside build stage"
      - npm test || echo "Tests failed or not defined, continuing..."
artifacts:
  files:
    - '**/*'
